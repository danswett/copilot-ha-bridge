# Local configuration and anything derived from it. The config file holds a Home
# Assistant long-lived access token and must never be committed.
config.json
agent-ha-bridge.config.json
*.local.json

# Logs and runtime state
*.log
agent-bridge-daemon-state.json

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini

# MCP server
mcp/node_modules/
